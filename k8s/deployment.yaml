apiVersion: apps/v1
kind: Deployment
metadata:
  name: brainboost
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: brainboost
  template:
    metadata:
      labels:
        app: brainboost
      annotations:
        rollout: "2026-04-15"
    spec:
      containers:
      - name: brainboost
        image: puriicyber/brainboost:latest
        ports:
        - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: brainboost-service
  namespace: default
spec:
  selector:
    app: brainboost
  ports:
  - protocol: TCP
    port: 80
    targetPort: 5000
  type: NodePort
